
export interface ProductType {
    sku: string;
    name: string;
    unit_price: number;
}

export interface ProductProps {
    product: ProductType;
}

const Products = () => {
    return (
        <div>

        </div>
    );
};

export default Products;